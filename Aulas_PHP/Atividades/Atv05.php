<?php 
        $a = 1;
        $b = 1;
        $c = "Nando";
        $d = "Nando";

        //Igual: ==
        $a == $b; //retorna TRUE

        //Identico: ===
        $a === $b; //retorna FALSE

        //Diferente: !=
        $a != $c; //retorna TRUE

        //Menor que: <
        $a < $b; //retorna FALSE

        //Maior que: >
        $a > $b; //retorna FALSE

        //Menor ou igual: <=
        $a <= $b; //retorna TRUE

        //Maior ou igual: >=
        $a >= $b; //retorna TRUE
        
?>
